# PostgreSQL service for Ory Hydra
apiVersion: v1
kind: Service
metadata:
  name: hydra-postgres
  namespace: hydra
  labels:
    app: hydra-postgres
spec:
  type: ClusterIP
  selector:
    app: hydra-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
