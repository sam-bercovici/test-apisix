# Optional secret example if you prefer referencing credentials via secretRef.
# Not required for the current deployment (see go-rest-consumer.yaml).
apiVersion: v1
kind: Secret
metadata:
  name: go-rest-consumer-secret
  namespace: default
type: Opaque
stringData:
  key: go-rest-demo-key
